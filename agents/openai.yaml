interface:
  display_name: "PPT Design DNA"
  short_description: "Adapt Design DNA into animated HTML decks"
  brand_color: "#111827"
  default_prompt: "Use $PPT-Design-DNA to create or reuse a versioned Design DNA profile, then collect PPT requirements with numbered A/B/C/D option panels instead of blank fields, handle content images through Image Asset Strategy, adapt it to my scenario, plan a blueprint, and generate a fixed-stage animated HTML deck with no fake image placeholders, readable text/surface pairs, and safe layering."

policy:
  allow_implicit_invocation: true
